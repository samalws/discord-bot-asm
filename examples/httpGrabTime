$addProc ```
# sends an http request to figure out what time it is using the worldtime API
# prints out the response and the response code

start:
LITSTR a http://worldtimeapi.org/api/timezone/America/Chicago
HTTP a err
CHN channel
STR a
STR err
SEND channel a
SEND channel err
EXIT
```
